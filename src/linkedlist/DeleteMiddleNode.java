package linkedlist;

public class DeleteMiddleNode {
	
	
	
	//http://stackoverflow.com/questions/17573120/finding-middle-element-of-the-linked-list
	
	//a->b->c->d->e
	/*  Working Logic, I commented it just to avoid errors showing up in the Eclipse package
	public static boolean deleteNode (middle c){
		
		//STEP 1 : Copy the data from the next node of the middle node (here d) to c node
		
		//  c next = c.next; 
		  
		// STEP 2: Copy the data value 
		  
		//  c.data = next.data;
		  
		  // STEP 3: Set the next pointer of the node to next node's next pointer ( Here set the pointer of c to node e basically
		  
		 // c.next = next.next;
		  
		  return true;
		*/
		
		
		
	//}

}
